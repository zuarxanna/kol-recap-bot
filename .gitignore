node_modules/
dist/
.env
*.log
out/

# Internal working docs — kept locally, not pushed (content folded into README.md)
CHECKPOINT.md
THREADS_POST.md

# Real master data — copy the *.example.json templates to create these.
# They hold real KOL handles, so they stay out of the repo.
db/model/Kol/kols.json
db/model/Campaign/campaigns.json
